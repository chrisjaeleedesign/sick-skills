# OpenAI API key for embedding generation (text-embedding-3-small, 1536 dims).
# If not set, semantic search is disabled and the system falls back to FTS-only search.
# Embedding generation is best-effort — thought creation never fails due to missing embeddings.
OPENAI_API_KEY=sk-...
