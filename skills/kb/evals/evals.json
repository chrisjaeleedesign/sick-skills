{
  "skill_name": "kb",
  "evals": [
    {
      "id": 1,
      "prompt": "/kb update",
      "expected_output": "The skill checks gws auth and KB config, runs the kb-update helper in dry-run mode first, and only writes the manifest with explicit confirmation or when the task calls for an update write.",
      "files": [],
      "assertions": [
        "Checks gws auth status",
        "Reads .agents/kb/config.json for folder_id and manifest_sheet_id",
        "Runs scripts/kb-update.mjs with --dry-run first",
        "Uses --write only for explicit manifest updates",
        "Reports added, updated, restored, removed, unchanged, and total counts"
      ]
    },
    {
      "id": 2,
      "prompt": "/kb what does our brand voice guide say about tone for customer emails?",
      "expected_output": "The skill runs scripts/kb-query.mjs with the workspace config and question, then answers from returned active sources with citations by source name.",
      "files": [],
      "assertions": [
        "Runs scripts/kb-query.mjs with --config and --question",
        "Uses active manifest rows only",
        "Answers grounded in doc content, not general knowledge",
        "Cites the source doc by name"
      ]
    }
  ]
}
